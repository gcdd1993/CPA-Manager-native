{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the main CPA Manager window",
  "windows": ["main"],
  "permissions": ["core:default"]
}

